# Docker Compose defaults. Keep real secrets in .env, never in Git.
MYSQL_DATABASE=hm_shopping_mart
MYSQL_USER=Sidd
MYSQL_PASSWORD=Sidd@123
MYSQL_ROOT_PASSWORD=root_password_change_me

# Optional image registry for Jenkins, e.g. docker.io/youruser
REGISTRY=docker.io/your-dockerhub-user
IMAGE_REPO=hm-shopping-mart
